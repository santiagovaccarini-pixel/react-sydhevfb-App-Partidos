{
  "name": "app-registro-partidos",
  "version": "2026.9.8",
  "private": true,
  "dependencies": {
    "@supabase/supabase-js": "^2.105.4",
    "react": "^18.1.0",
    "react-dom": "^18.1.0"
  },
  "scripts": {
    "predev": "node scripts/sync-version.js",
    "dev": "vite",
    "prestart": "node scripts/sync-version.js",
    "start": "vite",
    "prebuild": "node scripts/sync-version.js",
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "postbuild": "node scripts/precache.js"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  }
}
